# 代理回答・修正権限の検証

## 現在地

2026-09-10。修正ボタンを押したinfoアカウントの署名検証済み操作5件で403を確認。
カード利用者だけを許可する旧条件で拒否され、修正画面を開く前に処理が終わっていた。
過去13:57の回答者不一致は別の事象であり、原因未解明のまま保持する。

承認済みの「本人へメンションし、別の利用者も回答できる」方針に対応するコードを実装。
回答・修正機能のコード版 `bdb2a999-2a94-4963-a6c6-6768d38f2eef` を配置した。最新配信は [PoCのメンション先統一](mention-review.md) を参照。修正保存後に操作画面へ「修正しました」を表示する。
`responders.sql` は検証D1へ適用済み。ユーザーの明示承認後、infoアカウントを追加回答者として1件登録し、有効状態をSELECTで確認した。対応済みカードの初回回答・理由付き修正を行える。

infoのアカウントを追加回答者に登録する処理は、自動承認レビューに拒否された。
理由は対象Slack IDと代理回答・修正の権限範囲について明示承認がないこと。
その後、対象Slack IDと検証チャンネル内の対応済みカード全件への権限範囲を提示し、ユーザーの「OK」を受けて登録を実行した。

22:58（JST）、Codexがユーザーの開いているinfoのSlack画面で、該当投稿の「回答を修正する」をクリック。事業と修正理由のフォームが表示されたこと、署名検証済みのinfo操作にHTTP 200を返したこと、修正セッションにview IDが保存されたことを確認した。
この表示確認では分類変更を登録していない。続いて23:00（JST）にユーザーがinfoから修正を実行し、FP事業から全社共通への変更、修正理由、実操作ユーザーの監査保存とHTTP 200を確認した。元のFP経費を全社共通へ変えた修正テストである。

同じ既存投稿1件に対する表示更新を手動実行し、Slack API成功、outboxのsent、画面上の「分類済み：全社共通（回答者：Ryosuke Kojima（info））」を確認した。追加投稿・メンションは行わず、一時実行環境は停止済み。

当初は修正後に入力画面が閉じるだけだったため、「修正しました」の完了画面を追加した。保存済みの変更前後と理由を操作した人へ表示し、再送でも監査を増やさず同じ完了画面を返す。完了画面の追加後も、Slack表示更新キューの処理経路を維持する。新しい完了画面自体は自動テストで確認済みで、実Slackでの次回修正時の表示確認は未実施。

## 実装した権限と記録

- 有効なカード対応の本人、または同一ワークスペースの `poc_responders` に有効登録された回答者が、初回分類と修正を行える。
- ワークスペース、アプリ、チャンネル、元投稿、スレッド、カード対応の照合は維持する。未登録者は許可しない。
- 追加回答者には、検証チャンネルの対応済みカードの分類・理由付き修正を許可する。権限は取引1件だけに限定するものではない。
- メンション先はカード利用者のまま。回答者は署名検証済みの実操作IDから記録し、完了表示はその人の通常文字の名前を使う。
- 画面を開いた後に回答者権限を無効化した場合も、保存直前のSQL条件で拒否する。
- 他人が開いた修正セッションの引継ぎ、確認を飛ばした保存、同時修正での上書きは許可しない。
- 自動送信は停止、プレビューURLは無効のまま。修正後のSlack表示更新は別途手動実行する。

## 検証

`node --test tools/upsider-poc/*.test.mjs`：79件成功。
代理での分類・修正、依頼先と回答者の分離、メンションなしの回答者表示、
別ワークスペースの権限無効、権限撤回と保存の競合、他人のセッション拒否を含む。
単一ファイル生成後の構文チェックも成功。上記のとおり、実Slackでinfoによる修正登録と既存投稿の表示更新まで確認した。

要件定義書の本人限定・再通知後のみ代理可能という旧記述を、合意済みの最新方針に揃えた。
9事業の選択肢、登録確認、修正ボタン、完了時にメンションしない表示も同時に反映した。

## Slack投稿への修正理由の表示

分類済みの事業・回答者の下、修正ボタンの上に、最新の保存済み修正理由を通常文字で表示する。現在の事業・回答者・保存時刻に一致する修正履歴を参照し、初回分類には修正理由欄を出さない。理由内のメンション記法もplain_textとして扱う。

既存の検証投稿1件を更新し、Slack画面で「修正理由：修正テスト (元々FPの経費)」を確認。outboxはsent、一時実行環境は停止。79件の既存テストと配置用ファイルの構文チェックは成功した。自動送信停止の設定は維持している。
