# #14 新アプリ基盤の確認記録

実施者：Codex / 実施日：2026-09-15 / 対象：`apps/management-board/` のローカル環境。実業務データ、既存PoC、リモートのWorker・D1は使用していない。

## 結果

| 確認 | 結果 |
| --- | --- |
| ソースとlockfileだけを別の空ディレクトリへ複製し `npm ci` | 成功。Node26.7.0 / npm11.19.0。npm監査0件 |
| 複製先で `npm run check` | strict型検査、10テスト、ビルド成功 |
| 複製先で `npm run dev` | 4177で起動。画面直アクセス、Worker API、空のローカルD1へのSELECT成功 |
| ビルド済みプレビュー | 4178で起動。`npm run test:smoke` 成功 |
| ページルーティング | ホーム・入力確認・接続状況。直接URLを開いても起動 |
| APIの正常・異常 | データ形式、リクエストID、DB失敗503、未知API404、未実装の連携・締め503を確認 |
| フォーム | 空欄のエラーと入力欄へのフォーカス、日本語入力、前後空白の除去、保存しない確認結果をブラウザで確認 |
| ユーザー本文 | HTML風の文字列が文字として表示され、DOMに画像要素を生成せず、スクリプトのダイアログも出ないことを確認 |
| 環境の境界 | 検証／本番値、別アプリ名、外部ホストでは画面・APIを503。設定検査でリモートDBや秘密の混入を拒否 |
| HTTPヘッダー | HTML・API・エラーにCSP、no-store、nosniff、フレーム禁止。ビルド版でunsafe-inline / unsafe-evalなし |
| 画面幅 | PC1280px、ホーム390px、接続状況320px。スマートフォン幅のナビゲーションが使え、ページ全体の横はみ出しなし。表だけ横スクロール可 |
| ブラウザログ | 上記の確認時にerror / warnなし |
| Git除外 | `.dev.vars` / `.env` / `.wrangler` / `dist` の除外を確認 |

テストで扱うのは環境境界、D1未設定・失敗、SPAとAPIの分離、未設定処理の停止、入力検証、Origin・JSONサイズ・構文、例外本文の秘匿、ヘッダー。本番の認証・認可や業務処理が完成したことを示すテストではない。

ビルドのJavaScriptは約268KB（gzip約85KB）、Workerは約7.4KB。既存書体はFontsourceのUnicode分割ファイルとして同一オリジンから配信する。クリーン導入ではmacOS向け任意依存fseventsのインストールスクリプト未承認警告が出たが、依存導入・開発サーバー・ビルドは成功した。esbuild / workerdは固定した版のスクリプトを許可済み。

## このIssueで完了しないもの

- #13の要件責任者／役員による画面確認
- #15以降の業務テーブル、マイグレーション、保存、監査、同時編集制御
- #9・#16〜#18の実リソース分離、URL設定、Googleログイン、許可リスト・認可
- 目標・売上・経費等の業務入力、UPSIDER本実装、継続再通知、月次締め
- 本番配置、実ブラウザ機種ごとの網羅試験、想定データ量での性能測定

クラウドへの新規配置・契約・課金は行っていない。既存のSlack検証環境の設定・通知・回答データも変更していない。
