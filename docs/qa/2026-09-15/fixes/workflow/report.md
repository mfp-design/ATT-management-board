# 業務・管理画面の修正再検証

確認日: 2026-09-15 21:05〜21:11 JST / 確認者: workflow QA subagent

対象: `http://127.0.0.1:4176/`、root が修正したローカル画面案の作業ツリー。ソース変更なし。架空データのブラウザー状態のみ操作し、実サービス・Sites・公開・永続データは操作していない。

IAB が利用できなかったため、Chrome の新規専用タブを使用。既存のユーザータブ・viewport 設定は変更していない。検証後の役員承認や事業上の最終判断とは扱わない。

## 判定

| ID | 判定 | 実操作の結果 |
| --- | --- | --- |
| W01 同名別IDの統合 | PASS | 案件0件の新規同名IDを統合しても元の同名取引先と既存案件2件が不変。さらに新規同名IDに案件を1件作って統合すると、その1件だけ移る。 |
| 取引先編集 | PASS | 名称・種別の初期値、空白理由の拒否、内容確認後キャンセルで変更なし、保存時の変更前後・理由・実行者・日時、同一IDを保った案件参照更新を確認。 |
| 案件からの取引先変更 | PASS | 編集時の取引先初期選択は名称変更後のIDに一致。D-001をA-001→A-002に変更し、取引先履歴の前後ID・名称・理由・総務担当・日時を確認。 |
| W02 事業・メンバー無効化 | PASS | 両方で終了日が開始日前なら保存前に拒否。同日開始終了は許可される。無効化後の前後値を履歴に残し、メンバーの所属期間も終了させ、新規候補から除外。 |
| W03 所属期間・兼務履歴 | PASS | 兼務追加・削除の前後値、所属ごとの期間変更、在籍開始前・在籍終了後・期間逆転の拒否、同日境界、変更対象以外の所属維持、旧履歴保持を確認。 |

今回の対象範囲で新たな不具合は再現しなかった。ブラウザーの console error/warn は0件。

## 再現手順と証拠

### W01: ID単位の統合と対象外案件の維持

1. 総務担当で既存 `サンプル商事 / A-001` と同名の法人を追加し、類似候補を確認して別ID登録。
2. 新規 `A-1789473925812` のみを `サンプル企画 / A-002` へ統合。
3. 「ライフプラン継続支援」「保険相談 継続サポート」の取引先はサンプル商事のまま。元 `A-001` も維持される。
4. さらに別の同名ID `A-1789473962970` を登録し、これに「QA別IDの案件」10,000円を明示的に紐づけて作成。
5. この新規IDだけを `A-002` へ統合すると、QA案件だけがサンプル企画へ変更。既存の上記2案件はサンプル商事のまま。
6. QA案件詳細に旧ID→A-002・理由・実行者・日時が表示された。

期待・実際とも、統合したIDへの参照だけが変わる。`src/prototype-model.js` の `relinkAccounts` がIDだけで対象を判定し、`src/data.js` の初期案件もIDを持つことと整合。

証拠: `w01-isolated-merge.png`、`w01-only-linked-deal.png`、`evidence.json` の `W01-no-linked-deals`・`W01-only-linked-deal`。

### 取引先編集・参照更新

1. 総務担当でA-002を編集し、名称と種別を変更して理由を入力→内容を確認→キャンセル。
2. 元の名称「サンプル企画」、種別「法人」、ID A-002を維持し、変更履歴も増えない。
3. 再度編集し、名称「QA名称変更済み」、種別「個人」、理由を空白のみで確認しようとすると「変更理由を入力してください」。
4. 理由「QA：名称と種別を訂正」を入力して確定。
5. A-002のIDを維持して名称・種別が更新され、取引先履歴に `サンプル企画 / 法人 → QA名称変更済み / 個人`、理由、総務担当、日時を表示。
6. A-002を参照する元の2案件とQA案件だけに名称が反映。A-001側の案件は不変。
7. QA案件の詳細では旧統合履歴と名称変更履歴を両方保持。案件編集を開くと `A-002 / QA名称変更済み` が選択済み。

証拠: `evidence.json` の `Account-edit-cancel`・`Account-edit-save`・`Account-edit-linked-refs`・`Account-edit-form-selected`。

### 案件フォームから直接取引先を変更

1. 「ライフプラン継続支援」D-001の案件編集を開き、取引先を A-001→A-002（名称変更後）へ変更。
2. 変更理由「QA：案件編集で取引先を変更」を確認・確定。
3. 案件詳細の「取引先の変更履歴」に `A-001 / サンプル商事 → A-002 / QA名称変更済み`、理由、総務担当、2026/9/15 21:08:12を表示。

証拠: `account-direct-change-history.png`、`evidence.json` の `Deal-direct-account-change-history`。

### W02: 無効化の逆転・同日境界

1. システム担当で日本酒（開始2025-10-01）を終了2025-09-30で無効化しようとする→「終了日は開始日以降にしてください」で拒否。
2. 担当C（開始2025-10-01）でも同じ日付で拒否。
3. その後、担当Cは終了2025-10-01で無効化でき、主所属Agerunも同日終了へ更新され、変更前後の在籍・所属期間と理由・実行者・日時が履歴に残る。
4. 日本酒も終了2025-10-01で無効化できる。
5. 新規案件の候補から日本酒と担当Cが除外され、他事業・担当A/Bは残る。

証拠: `w02-inverted-rejected.png`、`evidence.json` の `W02-business-inverted`・`W02-member-inverted`・`W02-member-valid-boundary-history`・`W02-invalid-candidates-excluded`。

### W03: 各所属期間と前後値

1. 担当BにFP事業・企業研修の2兼務を追加→変更前には主所属MoneRunのみ、変更後には3所属それぞれの期間を表示。
2. `担当B・FP事業の所属期間を変更` で開始を在籍前2025-09-30にすると拒否。
3. 所属開始2026-09-11、終了2026-09-10も拒否。
4. 開始終了をともに2026-09-11にすると保存できる。FP事業だけが1日間へ変更され、MoneRunと企業研修の期間は不変。履歴に旧期間・新期間・理由・実行者・日時を表示。
5. 担当CのAgerun所属を2026-09-30終了にした後、メンバーの在籍終了も同日へ設定（境界一致を許可）。その後Agerunの所属終了を2026-10-01へ延ばす操作は在籍期間外として拒否。
6. 担当Bから企業研修の兼務を外して保存しても、FP事業の独自期間は維持され、履歴には企業研修が存在した旧状態と削除後の状態の両方を保持。

証拠: `w03-membership-history.png`、`evidence.json` の `W03-*`。

## 範囲の限界

- 関連するフォーム・状態遷移をChromeで実操作した回帰確認。全ブラウザー・実機・認証・永続DB・監査データの耐久性は対象外。
- 在籍・所属期間の過去時点集計を本実装したことを意味しない。今回確認したのは入力、対象外期間の拒否、個別期間の保持、表示履歴。
- 取引先編集の「取消」は確認画面でのキャンセルを検証した。取引先そのものの削除・取消機能は対象としていない。
- W03履歴のスクリーンショットはviewport内の一部のみ。全行の前後値は `evidence.json` に記録。
