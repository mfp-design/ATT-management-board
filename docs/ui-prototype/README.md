# Phase 1 画面案（作成途中）

対象: [Issue #13](https://github.com/mfp-design/ATT-management-board/issues/13)

中断時点: 2026年9月11日 / 作成・動作確認: Codex

既存画面の配色・書体を引き継ぎ、現在の要件に合わせて画面構成を具体化するためのローカルプロトタイプです。**全画面・全状態の作成と確認はまだ完了していません。要件責任者・役員の二段階レビューも未実施です。**

## 確認するには

開発サーバー起動中のURL: <http://127.0.0.1:4176/>

上部の「役割」で経営者・総務担当・システム担当を切り替え、「状態」で通常・空・読込中・通信エラー・競合・利用不可を試せます。変更はブラウザー内だけに保持し、再読み込みで架空の初期データに戻ります。表示期間のサンプルは2026年9月です。

サーバーが停止している場合の再開手順:

```sh
cd docs/ui-prototype
npm ci
npm run dev -- --host 127.0.0.1 --port 4176 --strictPort
```

この画面案は認証・DB・Slack・Cloudflareと接続していません。役割切替は表示設計の確認用であり、本番の認可検証ではありません。利用者登録など、一部の管理操作は確認画面までの仮動作です。本番への配置は行っていません。

## 基準と成果物

- 現在の要件: [要件定義書](../requirements/management-board-rebuild.md)
- デザイン基準: [現行画面の保存資料](../visual-reference/README.md)
- 画面ごとの作成状況: [screen-inventory.md](screen-inventory.md)
- 検証済みの操作・未確認事項: [design-qa.md](design-qa.md)
- 中断時のPC画面: [pc-dashboard-checkpoint.png](evidence/pc-dashboard-checkpoint.png)

紺色の背景、カード、書体、PCの左ナビゲーションを継承しています。スマートフォンではナビゲーションを開閉式に変更しました。指標と動線は現行画面の計算・情報構造を流用せず、現在の要件から組み直しています。

## 次回の再開順

1. `design-qa.md` に記載した既知の不整合を直し、直近の修正を再検証する。
2. 画面一覧と要件を突き合わせ、分割予定・実績調整・複数の次の行動・管理画面などの不足を補う。
3. PC・スマートフォンで一時アクセス、支援コメント、キーボード操作、エラー／競合を確認する。同じ表示条件で現行画面と比較し、最終スクリーンショットを保存する。
4. 全画面が揃った版で要件責任者の操作性・デザイン確認を受け、その後に役員の表示項目・判断用途の確認を受ける。確認者・日付・対象コミット・指摘と反映結果を記録する。

途中の保存であるため、Issue #13の完了条件はチェックしません。UPSIDER PoCの再通知テストは別作業です。この画面案の操作ではPoCの予約・データ・送信設定は変更されません。

## レビュー記録

| 段階 | 状態 | 確認者・日付・対象版 |
| --- | --- | --- |
| 要件責任者による操作性・既存デザイン継承の確認 | 未実施 | 未記録 |
| 役員による表示項目・判断への有用性の確認 | 未実施 | 未記録 |

## ビルド

```sh
npm run build
```

2026年9月11日の中断時点で成功。JavaScriptの単一チャンクが500 kBを超える警告は残っています。配信の最適化は未実施です。テンプレート付属の配信用ファイルは保持していますが、公開・デプロイの確認はしていません。
